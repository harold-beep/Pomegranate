import PomoButtons from "./PomoButtons";

const TimerButtons = () => {
  return (
    <div className="button-groups">
      <PomoButtons>timer one</PomoButtons>
      <PomoButtons>timer one</PomoButtons>
      <PomoButtons>timer one</PomoButtons>
    </div>
  );
};

export default TimerButtons;
